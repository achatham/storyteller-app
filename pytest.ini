[pytest]
testpaths = tests
# Tests import the repo-root packages (pipeline, webapp) directly; without this
# a bare `pytest` cannot find them (only `python -m pytest` would work).
pythonpath = .
addopts = -q
